name: Deploy Admin Live Dashboard

on:
  push:
    branches: ["main"]
    paths:
      - "docs/field-desk-admin-deploy/**"
      - ".github/workflows/admin-live-dashboard-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: admin-live-dashboard-pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload admin dashboard artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/field-desk-admin-deploy

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
