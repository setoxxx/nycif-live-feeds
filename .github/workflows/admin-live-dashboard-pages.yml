name: Deploy Admin Live Dashboard

# Retired. God View / admin calendar / platform-roadmap GitHub Pages are not
# a product surface. The phone reads Supabase. Do not re-enable this job.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "Admin GitHub Pages deploy is retired."
