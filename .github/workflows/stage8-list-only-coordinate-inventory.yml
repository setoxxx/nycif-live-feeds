name: Archived Stage 8 List-Only Coordinate Inventory

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Stage 8 completion notice
        run: |
          echo 'Stage 8 is complete and archived.'
          echo 'See data/reports/stage8_supported_coordinate_resolution_certificate.json.'
