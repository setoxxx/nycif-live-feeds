#!/usr/bin/env python3
"""Protected incoming-data residual, completion, and intake-resolution audit.

The script reads current source snapshots and writes protected evidence under a
fixed /tmp directory. It does not modify production feeds, WordPress, public map
files, approval state, credentials, deployment settings, plugin activation,
s