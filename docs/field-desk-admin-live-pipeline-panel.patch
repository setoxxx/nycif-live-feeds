From d4f85898768105f7b817d42d786587ac6c2746ef Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Mon, 13 Jul 2026 21:58:41 +0000
Subject: [PATCH] Add Live Pipeline panel to admin dashboard

Fetch read-only live counts from nycif-live-feeds: pipeline dashboard,
delta report, and multi-source coverage report. Shows current staged events,
newly added delta, GPS review tail, and calendar-only gap.
---
 admin/index.html                 |  10 +-
 admin/live-pipeline-panel-v01.js | 235 +++++++++++++++++++++++++++++++
 2 files changed, 242 insertions(+), 3 deletions(-)
 create mode 100644 admin/live-pipeline-panel-v01.js

diff --git a/admin/index.html b/admin/index.html
index a653f8e..12b567e 100644
--- a/admin/index.html
+++ b/admin/index.html
@@ -15,6 +15,7 @@
 <section class="panel"><h2>Operator guardrails</h2><div class="notice ok">This dashboard loads static JSON snapshots only. It has no write, deploy, publish, approval, rejection, geocoding, cache, WordPress, or mutation controls.</div><div class="notice">This GitHub Pages admin view is the unfiltered operator/press desk. The public /map can stay curated while this view shows the broader daily event universe for review and coverage planning.</div></section>
 <section class="panel" id="errors" hidden><h2>Snapshot loading errors</h2><div id="error-list"></div></section>
 <section class="panel"><h2>System Overview</h2><div id="overview" class="grid"></div></section>
+<section class="panel" id="live-pipeline-section"><h2>Live Pipeline — Backend Data (Current + Newly Added)</h2><div class="notice ok">Read-only fetch from nycif-live-feeds GitHub artifacts. Shows current staged counts, delta changes, and multi-source coverage gaps. No publish controls.</div><div id="live-pipeline-status" class="muted detail">Loading live pipeline data…</div><div id="live-pipeline"></div></section>
 <section class="panel"><h2>Project Status</h2><div id="project-status"></div></section>
 <section class="panel"><h2>Source Freshness</h2><div id="source-freshness"></div></section>
 <section class="panel" id="press-god-view-section"><h2>Press God View — Unfiltered Daily Event Universe</h2><div id="press-god-view"></div></section>
@@ -24,8 +25,9 @@
 <section class="panel"><h2>Live Map Review</h2><div class="notice ok">Read-only links and preview for reviewing the live public map. This does not modify WordPress, iframe/embed settings, or public map runtime.</div><div class="links" id="map-links"></div><div class="map-preview" id="map-preview"></div></section>
 <section class="panel" id="xri-status-section"><h2>XRI God View</h2><div id="xri"></div></section>
 <section class="panel"><h2>Warnings / Operator Notes</h2><ul id="notes"></ul></section>
-<p class="footer">Admin Dashboard Data Panels v1. Visibility-only static snapshot display. No public map runtime, production feed, service worker, deploy config, WordPress, geocoding, cache, auth, secret, cookie, or localStorage workflow is implemented here.</p>
+<p class="footer">Admin Dashboard Data Panels v1 + Live Pipeline panel v01. Visibility-only display. Live pipeline reads nycif-live-feeds status artifacts; local admin/data snapshots remain for TVPP triage samples.</p>
 </div>
+<script src="./live-pipeline-panel-v01.js"></script>
 <script>
 const resources=[['index','./data/index.json'],['projectStatus','./data/project-status.json'],['sourceFreshness','./data/source-freshness.json'],['tvppCandidates','./data/tvpp-candidates.json'],['tvppTriage','./data/tvpp-triage.json'],['tvppLocationReadiness','./data/tvpp-location-readiness.json'],['xriStatus','./data/xri-status.json']];
 const triageBuckets=['strong_assignment','possible_assignment','logistics_or_closure','low_value','needs_review'];
@@ -48,7 +50,7 @@ async function load(){return Promise.all(resources.map(async([k,p])=>{try{const
 function renderErrors(rows){const fails=rows.filter(r=>r[2]&&r[0]!=='xriStatus');id('errors').hidden=!fails.length;const box=clear(id('error-list'));fails.forEach(f=>box.append(el('div','notice danger',f[3]+': '+f[2])))}
 function countFresh(s){const out={};(s?.sources||[]).forEach(x=>out[x.freshness||'unknown']=(out[x.freshness||'unknown']||0)+1);return Object.entries(out).map(([k,v])=>k+': '+v).join(', ')||'unknown'}
 function locSummary(x){const c=x?.locationBucketCounts||{};const blocked=(c.needs_address_cleanup||0)+(c.intersection_or_route||0)+(c.missing_location||0);return blocked?blocked+' need work':(c.geocode_ready?'ready':'review snapshot')}
-function renderOverview(d,rows){const x=xriData(d),loaded=rows.filter(r=>r[1]).length,total=rows.length,c=d.tvppCandidates?.itemCount??d.tvppCandidates?.rowCount??'—',nr=d.tvppTriage?.bucketCounts?.needs_review??'—';clear(id('overview')).append(card('Admin snapshots available',loaded+'/'+total,loaded===total?'All required snapshots loaded.':'One or more snapshots failed.'),card('Operator view mode','unfiltered press desk','Broader than public /map; read-only.'),card('Source freshness status',countFresh(d.sourceFreshness),'From source-freshness.json.'),card('Daily candidate universe',c,'All loaded TVPP leads shown below.'),card('TVPP needs-review count',nr,'Operator triage bucket; not a publish gate.'),card('Location readiness status',locSummary(d.tvppLocationReadiness),'From readiness buckets.'),card('Live Feeds go-live runway',x.score,(x.currentPhase||'checkpoint unavailable')+'; next: '+(x.nextGate||'unavailable')+'.'),card('Public map posture','curated view','Public map may intentionally show less.'))}
+function renderOverview(d,rows){const x=xriData(d),loaded=rows.filter(r=>r[1]).length,total=rows.length,c=d.tvppCandidates?.itemCount??d.tvppCandidates?.rowCount??'—',nr=d.tvppTriage?.bucketCounts?.needs_review??'—',live=window.NYCIF_LIVE_PIPELINE_SUMMARY||{};clear(id('overview')).append(card('Live staged events',live.staged_feed_events!=null?Number(live.staged_feed_events).toLocaleString():'loading…','From nycif-live-feeds live pipeline panel.'),card('Newly added (delta)',live.newly_added_events!=null?Number(live.newly_added_events).toLocaleString():'loading…','Since previous staged snapshot.'),card('Calendar-only gap',live.calendar_only_unique_keys!=null?Number(live.calendar_only_unique_keys).toLocaleString():'loading…','Citywide calendar rows not yet in permit pipeline.'),card('Admin snapshots available',loaded+'/'+total,loaded===total?'All required local snapshots loaded.':'One or more local snapshots failed.'),card('Operator view mode','unfiltered press desk','Broader than public /map; read-only.'),card('Local TVPP sample universe',c,'Local admin/data snapshot (may be stale).'),card('Live GPS review tail',live.gps_review_queue!=null?Number(live.gps_review_queue).toLocaleString():'loading…','From backend row disposition.'),card('Live Feeds go-live runway',x.score,(x.currentPhase||'checkpoint unavailable')+'; next: '+(x.nextGate||'unavailable')+'.'))}
 function renderProject(p){const box=clear(id('project-status'));if(!p){box.append(el('div','empty','project-status.json did not load.'));return}const g=el('div','grid');g.append(card('Current phase','Admin Dashboard Data Panels v1'),card('Admin mode',p.adminMode),card('Event sources',p.eventSourcesStatus?.summary),card('Production feed writes',String(p.eventSourcesStatus?.productionFeedWrites??false)));box.append(g,el('h3','', 'Completed safe phases'));const ul=document.createElement('ul');['Admin data snapshots v0','Event Sources dev tooling','Static admin dashboard skeleton','Admin dashboard data panels v1'].forEach(x=>ul.append(el('li','',x)));box.append(ul,el('div','notice ok','Next safe step: post-deploy Admin Dashboard QA v1. Hold XRI-G4 and XRI-G12.'))}
 function renderSources(s){const box=clear(id('source-freshness'));if(!s){box.append(el('div','empty','source-freshness.json did not load.'));return}box.append(table([['Source',r=>r.source],['Dataset',r=>r.sourceDatasetId],['Status',r=>r.freshness],['Rows',r=>r.rowCount],['Leads',r=>r.leadCount],['Date range',r=>txt(r.dateRange?.min)+' to '+txt(r.dateRange?.max)],['Recommendation',r=>r.recommendation],['Warnings',r=>chips(r.warnings,'warn')]],s.sources||[]));if((s.documentedOnlySources||[]).length){box.append(el('h3','','Documented-only sources'),table([['Source',r=>r.source],['Dataset',r=>r.sourceDatasetId],['Status',r=>r.status],['Freshness',r=>r.freshness],['Recommendation',r=>r.recommendation]],s.documentedOnlySources))}}
 function triageById(t){const byId=new Map();(t?.items||[]).forEach(i=>{const key=i.lead?.eventId||i.lead?.sourceRecordId;if(key)byId.set(String(key),i.triage)});return byId}
@@ -63,7 +65,9 @@ function renderLoc(l){const box=clear(id('location-readiness'));if(!l){box.appen
 function renderMap(d){const x=xriData(d),links=clear(id('map-links'));[['GitHub Pages Field Desk map',x.links?.fieldDesk||fieldDeskUrl],['nycinfocus.com/map/ public curated map',x.links?.publicMap||mapUrl],['Live Feeds repo',x.links?.liveFeedsRepo],['Raw major feed',x.links?.rawMajorFeed],['Raw all feed',x.links?.rawAllFeed]].forEach(([name,url])=>{if(url)links.append(link(name,url))});const iframe=document.createElement('iframe');iframe.src=x.links?.publicMap||mapUrl;iframe.title='Read-only preview of nycinfocus.com/map/';iframe.loading='lazy';iframe.referrerPolicy='no-referrer';iframe.setAttribute('sandbox','allow-scripts allow-same-origin allow-popups');clear(id('map-preview')).append(iframe)}
 function renderXri(x){x=x||fallbackXriGodView;const box=clear(id('xri'));const hero=el('div','god-hero');const score=el('div','score-card');score.append(el('div','label',x.projectLabel||'Press God View / Live Feeds readiness'),el('div','score',x.score));const p=el('div','progress'),bar=document.createElement('span');bar.style.width=Math.max(0,Math.min(100,Number(x.percent)||0))+'%';p.append(bar);score.append(p,el('div','detail','Estimated '+(x.percent??'—')+' percent complete. Current state: '+txt(x.state)+'.'));const state=el('div','stat');state.append(el('div','label','Current checkpoint'),el('div','value',x.currentPhase),el('div','detail','Next: '+txt(x.nextGate)));hero.append(score,state);box.append(hero,el('div','notice violet',x.operatorPurpose||'Unfiltered operator visibility for press/event planning.'),el('div','notice warn',x.publicMapPolicy||'The public /map may intentionally show less than this operator view.'),el('div','notice warn',x.blocker));const links=el('div','links');[['Master roadmap',x.links?.masterRoadmap],['Canonical roadmap Markdown',x.links?.masterRoadmapMarkdown],['Roadmap JSON',x.links?.masterRoadmapJson],['Recovery prompt',x.links?.recoveryPrompt],['Live Feeds repo',x.links?.liveFeedsRepo],['Raw major feed',x.links?.rawMajorFeed],['Raw all feed',x.links?.rawAllFeed]].forEach(([name,url])=>{if(url)links.append(link(name,url))});box.append(links);const summary=el('div','grid');summary.append(card('Critical path','G96 -> validation -> feed/browser verification','Do not skip the local path recapture.'),card('Production posture','not yet 50/50','Requires explicit go/no-go approval.'),card('Safety mode','locked read-only','This admin page has no mutation controls.'),card('Target state','50/50','Verified feeds, public map, rollback note, no unintended mutation.'));box.append(summary);box.append(el('h3','','Runway from 41 to 50'));const runway=el('div','runway');(x.runway||[]).forEach(item=>{const n=item.score??item[0],name=item.name??item[1],desc=item.description??item[2],s=el('div','step');s.append(el('span','num',n+'/50'),el('strong','',name),el('div','detail',desc));runway.append(s)});box.append(runway);const cols=el('div','two-col');const path=el('div','stat');path.append(el('div','label','Critical path'),el('div','value','Next safest sequence'));const ul=document.createElement('ul');ul.className='mini-list';(x.criticalPath||[]).forEach(y=>ul.append(el('li','',y)));path.append(ul);const locks=el('div','stat');locks.append(el('div','label','Operator locks'),el('div','value','Unfiltered visibility cannot mutate production'));const ul2=document.createElement('ul');ul2.className='mini-list';(x.locks||[]).forEach(y=>ul2.append(el('li','',y)));locks.append(ul2);cols.append(path,locks);box.append(cols,el('h3','','Field Desk local XRI registry'),table([['Gate',r=>r.gate??r[0]],['Status',r=>r.status??r[1]],['Meaning',r=>r.meaning??r[2]]],x.fieldDeskRegistry||fallbackXriGodView.fieldDeskRegistry),el('div','notice violet','God View distinction: this panel tracks nycif-live-feeds go-live readiness and unfiltered operator visibility while preserving the Field Desk admin registry guardrails.'))}
 function renderNotes(d){const n=clear(id('notes')),x=xriData(d);['Admin snapshots are not production feed JSON.','This dashboard has no write, publish, approval, rejection, geocoding, or cache controls.','Press God View is intentionally broader than the public /map and is for coverage planning.',(x.projectLabel||'Live Feeds go-live view')+' is currently '+txt(x.score)+' and blocked on '+txt(x.blocker)].forEach(y=>n.append(el('li','',y)));const nr=d.tvppTriage?.bucketCounts?.needs_review||0;if(nr)n.append(el('li','',nr+' TVPP candidate row(s) are in needs_review.'))}
-load().then(rows=>{renderErrors(rows);const d=Object.fromEntries(rows.map(r=>[r[0],r[1]]));const x=xriData(d);id('snapshot-time').textContent='Snapshot: '+(x.generatedAt||d.projectStatus?.generatedAt||d.index?.generatedAt||'unavailable');renderOverview(d,rows);renderProject(d.projectStatus);renderSources(d.sourceFreshness);renderPressGodView(d.tvppCandidates,d.tvppTriage,d.tvppLocationReadiness,x);renderCandidates(d.tvppCandidates,d.tvppTriage);renderTriage(d.tvppTriage);renderLoc(d.tvppLocationReadiness);renderMap(d);renderXri(x);renderNotes(d)});
+function renderAll(rows){renderErrors(rows);const d=Object.fromEntries(rows.map(r=>[r[0],r[1]]));const x=xriData(d);if(!window.NYCIF_LIVE_PIPELINE_SUMMARY)id('snapshot-time').textContent='Snapshot: '+(x.generatedAt||d.projectStatus?.generatedAt||d.index?.generatedAt||'unavailable');renderOverview(d,rows);renderProject(d.projectStatus);renderSources(d.sourceFreshness);renderPressGodView(d.tvppCandidates,d.tvppTriage,d.tvppLocationReadiness,x);renderCandidates(d.tvppCandidates,d.tvppTriage);renderTriage(d.tvppTriage);renderLoc(d.tvppLocationReadiness);renderMap(d);renderXri(x);renderNotes(d)}
+load().then(renderAll);
+document.addEventListener('nycif-live-pipeline-ready',()=>load().then(renderAll));
 </script>
 </body>
 </html>
diff --git a/admin/live-pipeline-panel-v01.js b/admin/live-pipeline-panel-v01.js
new file mode 100644
index 0000000..cfca018
--- /dev/null
+++ b/admin/live-pipeline-panel-v01.js
@@ -0,0 +1,235 @@
+(() => {
+  const VERSION = 'live-pipeline-panel-v01';
+  const LIVE_FEEDS_BASE = 'https://raw.githubusercontent.com/setoxxx/nycif-live-feeds';
+  const BRANCH_CANDIDATES = ['main', 'cursor/multi-source-coverage-leap-5215'];
+
+  const PATHS = {
+    dashboard: 'status/nycif-live-pipeline-dashboard.json',
+    delta: 'data/live_delta_report.json',
+    coverage: 'data/reports/multi_source_coverage_report.json',
+  };
+
+  function fmtNum(value) {
+    return Number.isFinite(Number(value)) ? Number(value).toLocaleString() : '0';
+  }
+
+  function fmtPct(value) {
+    const n = Number(value);
+    return Number.isFinite(n) ? `${n.toFixed(1)}%` : '—';
+  }
+
+  function fmtTime(value) {
+    if (!value) return 'Unknown';
+    const date = new Date(value);
+    if (Number.isNaN(date.getTime())) return String(value);
+    return date.toLocaleString([], { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit' });
+  }
+
+  function esc(value) {
+    return String(value ?? '').replace(/[&<>"']/g, (ch) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[ch]));
+  }
+
+  async function fetchLiveFeedsJson(path) {
+    let lastError = null;
+    for (const branch of BRANCH_CANDIDATES) {
+      try {
+        const url = `${LIVE_FEEDS_BASE}/${branch}/${path}?v=${Date.now()}`;
+        const response = await fetch(url, { cache: 'no-store' });
+        if (!response.ok) {
+          lastError = new Error(`${branch}/${path}: HTTP ${response.status}`);
+          continue;
+        }
+        return { branch, payload: await response.json() };
+      } catch (error) {
+        lastError = error;
+      }
+    }
+    throw lastError || new Error(`Could not load ${path}`);
+  }
+
+  function progressBar(label, pct, detail) {
+    const width = Math.max(0, Math.min(100, Number(pct) || 0));
+    return `
+      <div class="stat">
+        <div class="label">${esc(label)}</div>
+        <div class="value">${esc(fmtPct(width))}</div>
+        <div class="progress"><span style="width:${width}%"></span></div>
+        ${detail ? `<div class="detail">${esc(detail)}</div>` : ''}
+      </div>
+    `;
+  }
+
+  function eventRow(event) {
+    const title = event.title || event.event_name || 'Untitled event';
+    const date = event.date || String(event.start_date_time || '').slice(0, 10) || 'Date TBA';
+    const borough = event.borough || event.event_borough || (Array.isArray(event.boroughs) ? event.boroughs.join(', ') : '') || 'Borough TBA';
+    const location = event.display_location || event.location || event.event_location || event.address || 'Location TBA';
+    const sourceId = event.source_event_id || event.event_id || '';
+    return `
+      <tr>
+        <td>${esc(date)}</td>
+        <td>${esc(title)}</td>
+        <td>${esc(borough)}</td>
+        <td>${esc(String(location).slice(0, 120))}${String(location).length > 120 ? '…' : ''}</td>
+        <td>${esc(sourceId || '—')}</td>
+      </tr>
+    `;
+  }
+
+  function renderPanel(root, data) {
+    const { dashboard, delta, coverage, branches } = data;
+    const counts = dashboard?.current_counts || {};
+    const bars = dashboard?.progress_bars || {};
+    const multi = dashboard?.multi_source || coverage?.overlap_analysis || {};
+    const freshness = dashboard?.freshness || {};
+    const added = Array.isArray(delta?.added_events) ? delta.added_events.slice(0, 5) : (dashboard?.samples?.newly_added_events || []).slice(0, 5);
+    const calendarOnly = (coverage?.samples?.calendar_only || dashboard?.samples?.calendar_only_events || []).slice(0, 5);
+
+    root.innerHTML = `
+      <div class="notice ok">Loaded from nycif-live-feeds branch(es): ${esc(branches.join(', '))}. Read-only — no publish or mutation controls.</div>
+      <div class="grid">
+        <div class="stat">
+          <div class="label">Current staged (map-ready QA feed)</div>
+          <div class="value">${fmtNum(counts.staged_feed_events)}</div>
+          <div class="detail">Staged manifest: ${esc(fmtTime(freshness.staged_manifest_generated_at_utc))}</div>
+        </div>
+        <div class="stat">
+          <div class="label">GPS-valid permits</div>
+          <div class="value">${fmtNum(counts.staged_with_valid_gps)}</div>
+          <div class="detail">${esc(fmtPct(bars.auto_gps_match_pct || 0))} auto-match</div>
+        </div>
+        <div class="stat">
+          <div class="label">GPS review tail</div>
+          <div class="value">${fmtNum(counts.gps_review_queue)}</div>
+          <div class="detail">${esc(fmtPct(bars.gps_review_tail_pct || 0))} of classified permits</div>
+        </div>
+        <div class="stat">
+          <div class="label">Newly added (last delta)</div>
+          <div class="value">${fmtNum(counts.newly_added_events ?? delta?.added_count ?? 0)}</div>
+          <div class="detail">Removed ${fmtNum(counts.removed_events ?? delta?.removed_count ?? 0)} · Changed ${fmtNum(counts.changed_events ?? delta?.changed_count ?? 0)}</div>
+        </div>
+      </div>
+
+      <h3>Pipeline progress</h3>
+      <div class="grid">
+        ${progressBar('Permit ingestion', bars.permit_ingestion_pct ?? 100, `${fmtNum(counts.classified_permit_rows)} classified rows`)}
+        ${progressBar('Auto GPS matching', bars.auto_gps_match_pct, `${fmtNum(counts.staged_with_valid_gps)} with valid GPS`)}
+        ${progressBar('GPS review tail', bars.gps_review_tail_pct, `${fmtNum(counts.gps_review_queue)} in review queue`)}
+        ${progressBar('Multi-source coverage', bars.multi_source_coverage_pct, 'Permits + citywide calendar overlap audit')}
+      </div>
+
+      <h3>Multi-source coverage</h3>
+      <div class="grid">
+        <div class="stat">
+          <div class="label">Permit rows (tvpp-9vvx)</div>
+          <div class="value">${fmtNum(coverage?.sources_compared?.permit_open_data?.current_future_rows ?? counts.classified_permit_rows)}</div>
+        </div>
+        <div class="stat">
+          <div class="label">Citywide calendar (nyc.gov/events API)</div>
+          <div class="value">${fmtNum(coverage?.sources_compared?.citywide_calendar_api?.current_future_rows ?? counts.citywide_calendar_rows)}</div>
+        </div>
+        <div class="stat">
+          <div class="label">Overlap (title + date)</div>
+          <div class="value">${fmtNum(multi.title_date_overlap_unique_keys)}</div>
+        </div>
+        <div class="stat">
+          <div class="label">Calendar-only gap</div>
+          <div class="value">${fmtNum(multi.calendar_only_unique_keys)}</div>
+          <div class="detail">Not yet in permit pipeline — manual review required</div>
+        </div>
+      </div>
+
+      <h3>Newly added events (top 5)</h3>
+      <div class="table-wrap">
+        <table>
+          <thead><tr><th>Date</th><th>Title</th><th>Borough</th><th>Location</th><th>Event ID</th></tr></thead>
+          <tbody>
+            ${added.length ? added.map(eventRow).join('') : '<tr><td colspan="5" class="empty">No newly added events in the latest delta report.</td></tr>'}
+          </tbody>
+        </table>
+      </div>
+
+      <h3>Calendar-only samples (not in permit pipeline)</h3>
+      <div class="table-wrap">
+        <table>
+          <thead><tr><th>Date</th><th>Title</th><th>Borough</th><th>Location</th><th>Event ID</th></tr></thead>
+          <tbody>
+            ${calendarOnly.length ? calendarOnly.map(eventRow).join('') : '<tr><td colspan="5" class="empty">No calendar-only samples in coverage report.</td></tr>'}
+          </tbody>
+        </table>
+      </div>
+
+      <div class="links" style="margin-top:12px">
+        <a href="https://github.com/setoxxx/nycif-live-feeds/blob/main/status/nycif-live-pipeline-dashboard.json" target="_blank" rel="noopener noreferrer">Dashboard JSON</a>
+        <a href="https://github.com/setoxxx/nycif-live-feeds/blob/main/data/live_delta_report.json" target="_blank" rel="noopener noreferrer">Delta report</a>
+        <a href="https://github.com/setoxxx/nycif-live-feeds/blob/main/data/reports/multi_source_coverage_report.json" target="_blank" rel="noopener noreferrer">Coverage report</a>
+        <a href="https://raw.githubusercontent.com/setoxxx/nycif-live-feeds/main/data/nycif_staged_live_events.json" target="_blank" rel="noopener noreferrer">Staged feed (raw)</a>
+      </div>
+      <div class="links" style="margin-top:8px">
+        <button type="button" id="livePipelineRefreshV01" style="cursor:pointer;border:1px solid rgba(255,255,255,.12);border-radius:999px;padding:8px 14px;background:#182235;color:#60a5fa;font:inherit">Refresh live data</button>
+      </div>
+      <div class="detail" style="margin-top:8px">Last loaded: ${esc(fmtTime(dashboard?.generated_at_utc || delta?.generated_at_utc))}</div>
+    `;
+
+    document.getElementById('livePipelineRefreshV01')?.addEventListener('click', () => loadLivePipeline(true));
+  }
+
+  async function loadLivePipeline(force) {
+    const root = document.getElementById('live-pipeline');
+    const status = document.getElementById('live-pipeline-status');
+    const badge = document.getElementById('snapshot-time');
+    if (!root) return;
+
+    if (status) status.textContent = force ? 'Refreshing live pipeline data…' : 'Loading live pipeline data from nycif-live-feeds…';
+    root.innerHTML = '';
+
+    try {
+      const [dashboardResult, deltaResult, coverageResult] = await Promise.all([
+        fetchLiveFeedsJson(PATHS.dashboard),
+        fetchLiveFeedsJson(PATHS.delta),
+        fetchLiveFeedsJson(PATHS.coverage),
+      ]);
+
+      const branches = [...new Set([dashboardResult.branch, deltaResult.branch, coverageResult.branch])];
+      renderPanel(root, {
+        dashboard: dashboardResult.payload,
+        delta: deltaResult.payload,
+        coverage: coverageResult.payload,
+        branches,
+      });
+
+      const staged = dashboardResult.payload?.current_counts?.staged_feed_events;
+      if (badge && staged != null) {
+        badge.textContent = `Live: ${fmtNum(staged)} staged · ${fmtTime(dashboardResult.payload.generated_at_utc)}`;
+        badge.className = 'badge ok';
+      }
+      if (status) {
+        status.textContent = `Live pipeline loaded from ${branches.join(', ')}.`;
+      }
+
+      window.NYCIF_LIVE_PIPELINE_SUMMARY = {
+        staged_feed_events: dashboardResult.payload?.current_counts?.staged_feed_events,
+        newly_added_events: dashboardResult.payload?.current_counts?.newly_added_events ?? deltaResult.payload?.added_count,
+        gps_review_queue: dashboardResult.payload?.current_counts?.gps_review_queue,
+        calendar_only_unique_keys: dashboardResult.payload?.multi_source?.calendar_only_unique_keys
+          ?? coverageResult.payload?.overlap_analysis?.calendar_only_unique_keys,
+      };
+      document.dispatchEvent(new CustomEvent('nycif-live-pipeline-ready'));
+    } catch (error) {
+      root.innerHTML = `<div class="notice danger">Could not load live pipeline artifacts from nycif-live-feeds. Merge PR #149 on the backend repo and run generate_live_pipeline_dashboard_status.py, then refresh.<br><br>${esc(error.message || error)}</div>`;
+      if (status) status.textContent = 'Live pipeline unavailable.';
+    }
+  }
+
+  function init() {
+    loadLivePipeline(false);
+  }
+
+  window.NYCIF_LIVE_PIPELINE_PANEL = { version: VERSION, refresh: () => loadLivePipeline(true) };
+
+  if (document.readyState === 'loading') {
+    document.addEventListener('DOMContentLoaded', init);
+  } else {
+    init();
+  }
+})();
-- 
2.43.0

